import { View, Text } from "react-native";
import { styles } from "../styles";

export default function Chess() {
    return (
        <View style={styles.container}>
            <Text style={styles.text}>WELCOME TO CHESS</Text>
        </View>
    );
}